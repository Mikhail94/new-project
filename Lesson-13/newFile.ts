/**
 * nweFunc get parameters and check that this parameters are in the car    /// пример описания                                                                       /// ts doc                              
 * @param color - color of car                                             /// описание                          /// переменный. мы их описываем
 * @param number - numbers of wheels in the car                            /// описание
 * @param array                                                             описание
 * @param obj 
 * @returns - check if car the same color                                  ///описание
 */
function newFunc(color: string, number: number, array: string[], obj: any): boolean {
    return true;
}